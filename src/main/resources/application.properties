spring.application.name=SystemDesign-1

spring.datasource.username= figes

spring.datasource.password = lawda

spring.datasource.url = jdbc:postgresql://${DB_HOST}:${DB_PORT}/job_db

spring.jpa.hibernate.ddl-auto = update

logging.level.root = warn